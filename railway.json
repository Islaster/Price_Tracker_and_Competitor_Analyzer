{
    "services": [
      {
        "name": "backend",
        "rootDir": "server",
        "startCommand": "env/bin/python -m uvicorn run:app --reload",
        "installCommand": "pip install -r requirements.txt"
      }
    ]
  }
  